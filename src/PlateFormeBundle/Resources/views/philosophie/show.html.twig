{% extends 'base.html.twig' %}

{% block body %}
    <h1>Philosophie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ philosophie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ philosophie.name }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ philosophie.texte }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('philosophie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('philosophie_edit', { 'id': philosophie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
