{% extends 'base.html.twig' %}

{% block body %}
    {#<header id="_template_header">#}
        {#{{ render(controller('HarasBundle:Page:getPage'),{'name':'header'}) }} #}
    {#</header>#}
    <h1>Liste des articles de la page</h1>

    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Voir</th>
                <th>Editer</th>
            </tr>
        </thead>
        <tbody>
            {% for article in articles %}
                <tr>
                    <td>{{ article.name }}</td>
                    <td>
                        <a href="{{ path('article_show', { 'id': article.id }) }}">Visualiser</a>
                    </td>
                    <td>
                        <a href="{{ path('article_edit', { 'id': article.id }) }}">Editer</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <table>
        <tbody>
            <tr>
                <td>
                    <a href="{{ path('article_new') }}">
                        <input type="submit" value="Ajouter un nouvel article">
                    </a>
                </td>
            </tr>
            <tr>
                <td>
                    <a href="{{ path('page_index') }}">
                        <input type="submit" value="Retourner à la liste des pages">
                    </a>
                </d>
            </tr>
        </tbody>
    </table>
{% endblock %}
