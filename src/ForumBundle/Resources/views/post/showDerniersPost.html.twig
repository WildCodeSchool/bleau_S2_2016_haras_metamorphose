{# Dernier post de chaque catégorie #}
  <div class="container lastPost">
     <h2 class="soustitrePage center">Dernières discussions</h2>
        {# Ligne 1 #}
        <div class="row">
            {% if  lastPostCat1 is not empty %}
            <div class="col s12 m6">
              <div class="card blue-grey darken-1">
                <div class="card-content white-text colCustom">
                  <span class="card-title titreDerPost">{{ lastPostCat1[0].titre|capitalize }}</span>
                  <div class="lastPostContenu">{{ lastPostCat1[0].contenu|raw }}</div>
                </div>
                <div class="card-action">
                  <a class="waves-effect waves-teal btn-flat center indexEntrerFil"
                       href="{{ path('post_showAllPost', { 'id': lastPostCat1[0].id }) }}">
                        <i class="material-icons">input</i>
                  </a>
                  <p class="pDernierPost white-text">Message de : {{ lastPostCat1[0].user }} <br />
                  Posté le {{ lastPostCat1[0].dateCreate|date("d/m/Y") }}</p>
                </div>
              </div>
            </div>
            {% endif %}
            {% if  lastPostCat2 is not empty %}
            <div class="col s12 m6">
              <div class="card blue-grey darken-1">
                <div class="card-content white-text colCustom">
                  <span class="card-title titreDerPost">{{ lastPostCat2[0].titre|capitalize }}</span>
                  <div class="lastPostContenu">{{ lastPostCat2[0].contenu|raw }}</div>
                </div>
                <div class="card-action">
                  <a class="waves-effect waves-teal btn-flat center indexEntrerFil"
                       href="{{ path('post_showAllPost', { 'id': lastPostCat2[0].id }) }}">
                        <i class="material-icons">input</i>
                  </a>
                  <p class="pDernierPost white-text">Message de : {{ lastPostCat2[0].user }}<br />
                  Posté le {{ lastPostCat2[0].dateCreate|date("d/m/Y") }}</p>
                </div>
              </div>
            </div>
            {% endif %}
         </div> {# Fin 1ere ligne #}
        {# Ligne 2 #}
        <div class="row">
            {% if  lastPostCat3 is not empty %}
            <div class="col s12 m6">
              <div class="card blue-grey darken-1">
                <div class="card-content white-text colCustom">
                  <span class="card-title titreDerPost">{{ lastPostCat3[0].titre|capitalize }}</span>
                  <div class="lastPostContenu">{{ lastPostCat3[0].contenu|raw }}</div>
                </div>
                <div class="card-action">
                  <a class="waves-effect waves-teal btn-flat center indexEntrerFil"
                       href="{{ path('post_showAllPost', { 'id': lastPostCat3[0].id }) }}">
                        <i class="material-icons">input</i>
                  </a>
                  <p class="pDernierPost white-text">Message de : {{ lastPostCat3[0].user }}<br />
                  Posté le {{ lastPostCat3[0].dateCreate|date("d/m/Y") }}</p>
                </div>
              </div>
            </div>
            {% endif %}
            {% if  lastPostCat4 is not empty %}
            <div class="col s12 m6 ">
              <div class="card blue-grey darken-1">
                <div class="card-content white-text colCustom">
                  <span class="card-title titreDerPost">{{ lastPostCat4[0].titre|capitalize }}</span>
                  <div class="lastPostContenu">{{ lastPostCat4[0].contenu|raw }}</div>
                </div>
                <div class="card-action">
                  <a class="waves-effect waves-teal btn-flat center indexEntrerFil"
                       href="{{ path('post_showAllPost', { 'id': lastPostCat4[0].id }) }}">
                        <i class="material-icons">input</i>
                  </a>
                  <p class="pDernierPost white-text">Message de : {{ lastPostCat4[0].user }}<br />
                  Posté le {{ lastPostCat4[0].dateCreate|date("d/m/Y") }}</p>
                </div>
              </div>
            </div>
            {% endif %}
        </div>
  </div>

{% block javascripts %}

{#{{ parent() }}#}

<script>


</script>

{% endblock %}