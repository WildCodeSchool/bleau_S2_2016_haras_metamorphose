{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorieplateforme</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoriePlateforme.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categoriePlateforme.nom }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{% if categoriePlateforme.actif %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorieplateforme_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorieplateforme_edit', { 'id': categoriePlateforme.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
